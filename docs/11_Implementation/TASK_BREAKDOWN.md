# Task Breakdown

[SOURCE OF TRUTH]
Status: Frozen

Nguyên tắc chia nhỏ sprint thành task — không liệt kê task cụ thể ở đây (sẽ làm khi bắt đầu từng sprint thật, tránh lập kế hoạch chi tiết cho việc chưa tới).

## Nguyên tắc
1. Mỗi task phải trỏ về đúng một mục trong MODULE.md (Public Contract, Responsibilities...) — không tạo task không có căn cứ trong Bible.
2. Một task không được gộp hai module khác nhau — khớp FOLDER_RULES.md (mỗi module một thư mục, không trộn code).
3. Task liên quan tới một mục trong PENDING_DECISIONS.md phải trích rõ ADR nào đã resolve mục đó.

## Điều file này không định nghĩa
Không liệt kê task cụ thể — task thật được tạo khi mở từng sprint, dùng nguyên tắc ở đây làm khuôn.
