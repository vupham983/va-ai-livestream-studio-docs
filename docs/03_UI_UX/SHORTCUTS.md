# Shortcuts

[SOURCE OF TRUTH]
Status: Frozen

## Nguyên tắc
1. Mọi hành động Emergency Override phải có phím tắt riêng, và phím tắt đó phải hoạt động ngay cả khi một Dialog đang mở (khớp DIALOGS.md — Dialog không được modal đối với global safety controls).
2. Phím tắt được phân loại phạm vi: global (hoạt động ở mọi màn hình) hoặc contextual (chỉ hoạt động trong một vùng cụ thể) — mọi Emergency Override phải ở phạm vi global.
3. Phải có cơ chế phát hiện xung đột phím tắt trước khi gán — danh sách phím tắt cụ thể chưa chốt ở giai đoạn tài liệu này.

## Điều file này không định nghĩa
Không liệt kê bảng phím tắt đầy đủ — chờ giai đoạn thiết kế UI thật.
