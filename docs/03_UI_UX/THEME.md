# Theme

[SOURCE OF TRUTH]
Status: Frozen

Design tokens ở mức khái niệm — không phải giá trị màu/font cụ thể (đó là design system, giai đoạn riêng sau Bible).

## Khái niệm cần có (không định giá trị)
- Token màu theo ngữ nghĩa trạng thái (VD: màu "cảnh báo" dùng nhất quán cho mọi cảnh báo Health Monitor xuyên suốt UI, không mỗi Dock một màu riêng).
- Token phân biệt trực quan giữa Workspace Mode và Live Operations Mode (người dùng luôn biết đang ở đâu, quan trọng vì không được sửa Configuration ảnh hưởng runtime khi đang Live).

## Nguyên tắc
Đây là SoT duy nhất cho khái niệm design tokens — khi có design system thật (Figma hay tương đương), design system đó phải tuân theo khái niệm đã định nghĩa ở đây, không tự phát minh hệ thống tokens riêng.

## Điều file này không định nghĩa
Không định nghĩa mã màu, font family cụ thể.
